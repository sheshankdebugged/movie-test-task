/** @type {import('next').NextConfig} */
const nextConfig = { 
    images: {
        domains: ['repair-tech-connect-data.s3.amazonaws.com'], // Add the domains where your images are hosted
      },  
}

module.exports = nextConfig
    